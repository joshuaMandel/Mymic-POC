# Copy this file to .env.local and add your key to enable AI-generated
# match explanations (Claude Haiku 4.5). Without it, the app still works
# and falls back to templated explanations.
#
# Get a key at https://platform.claude.com  →  API keys  →  Create key
ANTHROPIC_API_KEY=
